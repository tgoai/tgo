Generic single-database configuration.

This directory contains Alembic migration scripts for tgo-vision-agent.

Tables:
- va_inbox: Message inbox for UI automation (inbound/outbound messages)
- va_sessions: AgentBay session management (cloud phone/desktop sessions)
- va_message_fingerprints: Message deduplication fingerprints

To run migrations:
    alembic upgrade head

To create a new migration:
    alembic revision -m "description"

To auto-generate migration from model changes:
    alembic revision --autogenerate -m "description"
